interface:
  display_name: "Message Parser"
  short_description: "Parse WhatsApp exports into normalized message objects."
  default_prompt: "Parse this WhatsApp export into structured messages with timestamp, sender, and content only."
